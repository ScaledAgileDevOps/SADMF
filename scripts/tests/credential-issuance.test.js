import { describe, it } from 'vitest'

// Scenarios from scripts/features/credential-issuance.feature
// Implement one scenario at a time. Read PROGRESS.md to find the next one.

describe('Badge credential issuance', () => {
  it.todo('Issue a credential to a new recipient')
  it.todo('Skip a recipient who already has a credential')
  it.todo('Issue credentials for multiple badge levels independently')
  it.todo('Fail visibly when a credential cannot be signed')
})
