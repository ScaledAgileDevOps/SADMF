import { describe, it } from 'vitest'

// Scenarios from scripts/features/credential-structure.feature
// Implement one scenario at a time. Read PROGRESS.md to find the next one.

describe('Open Badges 3.0 credential structure', () => {
  it.todo('Credential includes required JSON-LD contexts')
  it.todo('Credential declares required types')
  it.todo('Credential id is a unique, resolvable URL that identifies the issued credential')
  it.todo('Credential subject achievement references the correct achievement URL')
  it.todo('Credential issuer references the issuer profile URL')
  it.todo('Credential issuance date reflects the date the badge was awarded')
})
