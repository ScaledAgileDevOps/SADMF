{
  "name": "badge-issuer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "js-yaml": "^5.4.1",
    "jose": "^5.0.0",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
